<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 1. The Blue-Purple Gradient Background -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="45"
                android:endColor="@color/purple_gradient_end"
            android:startColor="@color/accent_blue"
            android:type="linear" />
            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- 2. The Inner White Box (Leaves a 2dp gap for the border) -->
    <item
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
        android:top="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <corners android:radius="10dp" />
        </shape>
    </item>
</layer-list>